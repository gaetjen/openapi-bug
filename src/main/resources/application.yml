micronaut:
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
      swagger-ui:
        paths: classpath:META-INF/swagger/views/swagger-ui
        mapping: /customPath/**
        #mapping: /swagger-ui/**
  application:
    name: openapiBug
netty:
  default:
    allocator:
      max-order: 3
